# Codi Full Audit

- **Date**: 2026-05-11 21:32
- **Document**: 20260511*213208*[AUDIT]\_codi-full-audit.md
- **Category**: AUDIT
- **Scope**: Whole repository (~140k LOC src + 54k LOC tests + 25k LOC templates)
- **Method**: 7 parallel read-only subagents (cli, core, runtime, templates, tests, config, cross-cutting)
- **Output**: severity-ranked findings, no edits applied

## Executive Summary

| Severity   | Count | Status  |
| ---------- | ----- | ------- |
| CRITICAL   | 1     | block   |
| HIGH       | 18    | warning |
| MEDIUM     | 22    | info    |
| LOW / INFO | 25    | note    |

Healthy baseline:

- 0 real FIXME/HACK in production source
- 30 rules + 73 skills carry full frontmatter, no drift
- `strict: true` TS, ESLint flat config with `no-explicit-any`, gitleaks + husky enforced
- `pnpm-lock.yaml` only (no manager mixing)
- `node_modules/` inside `src/templates/skills/content-factory/scripts/` is **NOT tracked** — false alarm from initial scan; `.gitignore` covers it

Top blocking issue:

- **CRITICAL path-traversal in brain-ui restore endpoints** — same-origin attacker can clobber arbitrary directories

---

## CRITICAL (1)

| #   | File:Line                                                                  | Finding                                                                                                                                                                                                                                                                               | Fix                                                                                                                        |
| --- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| C1  | `src/runtime/brain-ui/routes-api.ts:363-372` + `pages/settings.ts:266-279` | `:hash` and `:ts` path params flow into `path.join(homedir(), PROJECT_DIR, EXTERNAL_ARCHIVE_DIR, hash, ts)` then `restoreFromBackupDir`. `..%2F..%2F..` escapes the backup tree and lets same-origin browser tab restore arbitrary directories as "backup", clobbering project files. | Validate `ts` against `^\d{8}T\d{6}Z?$` and `hash` against `^[a-f0-9]{8,64}$` before joining; reject with 400 on mismatch. |

## HIGH (18)

### CLI surface (`src/cli/`)

| #   | File:Line                                                           | Finding                                                                                                                      | Fix                                                                                 |
| --- | ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| H1  | `cli/contribute-git.ts` + `cli/contribute.ts` (24 sites)            | `execFileAsync` git/gh calls have no timeout (only 1 of 24 has `timeout: 15_000`). Hung remote blocks CLI indefinitely.      | Add `src/utils/exec.ts` wrapper with default timeout + `AbortSignal.timeout(N)`.    |
| H2  | `cli/update.ts:284`, `cli/preset.ts:194`, `cli/preset-github.ts:91` | `git clone --depth` calls with no timeout.                                                                                   | Same shared wrapper with longer default (60s) + `--git-timeout` global flag.        |
| H3  | `cli/brain.ts:440,451,469`                                          | `handleOutput(result, …)` called but `process.exit(result.exitCode)` omitted — failed brain subcommands return exit 0 to CI. | Append `process.exit(result.exitCode)` after every `handleOutput`.                  |
| H4  | `cli/migrate.ts:106`                                                | Same as H3 — failures look like success.                                                                                     | Append `process.exit(result.exitCode)`.                                             |
| H5  | `cli/add.ts:40-344`                                                 | `--all` branch + name validation + `regenerateConfigs` copy-pasted 4 times (rule/skill/agent/mcp).                           | Extract `runAddAll(kind, handler, templates)` + `requireNameOrError(kind)` helpers. |

### Core engine (`src/core/`)

| #   | File:Line                                                                                                       | Finding                                                                                                                                                                         | Fix                                                                                              |
| --- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| H6  | `src/core/scaffolder/{skill,agent,rule,mcp}-scaffolder.ts`                                                      | 4 scaffolders duplicate `NAME_PATTERN_STRICT`, `createError`, force-check + write block, template-replace logic (~80% identical between skill/agent).                           | Extract `scaffolder/common.ts` with `validateArtifactName`, `writeIfNotExists`, `applyTemplate`. |
| H7  | `src/core/migration/{claude-md,agents-md}.ts:1-130`                                                             | Byte-for-byte identical except `CLAUDE.md`↔`AGENTS.md` strings + 1-line splitter diff.                                                                                          | Collapse into `importMarkdownConfig(projectRoot, filename, sectionStrategy)`.                    |
| H8  | `src/core/config/parser.ts:178-275`                                                                             | 3 near-identical try/catch + `parseFrontmatter` + `safeParse` + `zodToProjectErrors` blocks across `parseAgentFile`, `parseLegacyBrandFile`, `parseSkillFile`, `parseRuleFile`. | Extract generic `parseArtifactFile<Schema>(filePath, schema, mapper)`.                           |
| H9  | `core/version/artifact-manifest.ts:3,169` + `core/external-source/discovery.ts:3,161` vs `utils/frontmatter.ts` | Two competing frontmatter parsers (`gray-matter` in 2 files, internal `parseFrontmatter` in ~15 + CLI). Inconsistent error shape.                                               | Standardize on `#src/utils/frontmatter.js`; drop `gray-matter` dependency.                       |
| H10 | `src/core/docs/renderers/schema-renderers.ts:22-211`                                                            | `eslint-disable no-explicit-any` with 6+ `any` casts on Zod internals. Violates `codi-typescript` rule.                                                                         | Define minimal `ZodDef` typed interface; replace `any` with `unknown` + narrowing.               |
| H11 | `core/preset/preset-loader.ts:95,121`, `hooks/yaml-document.ts:92`, `backup/backup-manifest.ts:34`              | `as unknown as` double-casts bypass type safety; L95 re-casts error `Result` to success type.                                                                                   | Use discriminated-union helpers (e.g., `propagateErr`) rather than re-casting.                   |

### Runtime (`src/runtime/`)

| #   | File:Line                                                                 | Finding                                                                                                                                                                                                                          | Fix                                                                                                                   |
| --- | ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| H12 | `runtime/capture/{stop-hook.ts:64, prompt-hook.ts:38, tool-hook.ts:54}`   | Hook entry points trust `StopHookInput` as typed but upstream CLI parses raw JSON. No `sessionId/cwd` shape check — malformed payload causes `deriveProjectId` to throw silently.                                                | Validate at CLI parse site with Zod; fail open with explicit stderr message.                                          |
| H13 | `runtime/capture/stop-hook.ts:135,138,348-362`; `tool-hook.ts:116-126`    | `readActiveWorkflowId()` opens fresh `BrainEventLog`+`Database` 3× per Stop fire. SQLite handle churn + re-running migration probe on hot path.                                                                                  | Cache result with `BrainEventLog.wrap(handle)` at top of `processStopHook`/`processPostToolUse`.                      |
| H14 | `runtime/capture/stop-hook.ts:64-172`                                     | Steps 1-7 (ensureProject, ensureSession, openTurn, persistMarkers, closeTurn, refreshCaptureCount, aggregateSessionUsage) are 6-8 prepared writes with no enclosing `raw.transaction()`. Crash mid-flow leaves orphan turn rows. | Wrap in `handle.raw.transaction(() => { ... })()`; keep best-effort try/catch around `aggregateSessionUsage` outside. |
| H15 | `runtime/capture/{stop-hook.ts:320, prompt-hook.ts:66, tool-hook.ts:252}` | `deriveProjectId` copy-pasted in 3 hook files. Drift will produce divergent `project_id` for same cwd.                                                                                                                           | Extract to `capture/project-id.ts` and import.                                                                        |

### Tests

| #   | File:Line                                                                                                                                     | Finding                                                                                                                               | Fix                                                                                                                          |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| H16 | `tests/unit/cli/add-handlers.test.ts:27-65` (13 mocks), `hub-handlers.test.ts:30-123` (23 mocks), `init-wizard-paths.test.ts:43-55` (4 mocks) | 30+ `vi.mock` calls against `#src/core/*` and `#src/cli/*` modules. Violates `codi-testing` rule "do not mock the module under test". | Replace with in-memory fs fixtures + real scaffolders; mock only `@clack/prompts`. Convert hub-handlers to integration test. |
| H17 | `tests/unit/hooks/hook-dep-installer.test.ts` (16 calls), `init-wizard-modify-add.test.ts` (16 calls)                                         | Highest `toHaveBeenCalledTimes/With` density — asserts internal call counts vs outputs.                                               | Assert resulting state (files written, manifest entries) instead.                                                            |

### Config + Cross-cutting

| #   | File:Line                           | Finding                                                                                                                                                                                                                                                 | Fix                                                                                                                                     |
| --- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| H18 | `package.json:81,88,97,112,116-120` | Suspect unused deps: `@google/generative-ai`, `docx`, `openai`, `drizzle-kit`, `pdf-lib`, `pptxgenjs`, `jsdom`, `pagefind`. Zero `from "<pkg>"` imports in `src/`/`tests/`/`scripts/`. May be loaded by skill runtime scripts (excluded from tsconfig). | Run `pnpm dlx depcheck`. Move skill-runtime deps to `optionalDependencies` or per-skill — they ship in npm tarball as user-facing deps. |

## MEDIUM (22)

| #   | File:Line                                                                                                                                             | Finding                                                                                                                                     | Fix                                                                                        |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| M1  | `cli/init-helpers.ts`, `installed-artifact-inventory.ts`, `cli/update.ts` vs `cli/contribute.ts`                                                      | Two frontmatter parsers in parallel (raw `matter()` + `parseFrontmatter`).                                                                  | Standardize on `parseFrontmatter`.                                                         |
| M2  | `cli/docs.ts:119,131`                                                                                                                                 | Subcommand `--json` collides with global `--json`.                                                                                          | Rename catalog flag to `--catalog-json`; restore unconditional `handleOutput`.             |
| M3  | `cli/hooks-list.ts:69-75`                                                                                                                             | Writes directly to `process.stdout`, bypasses `handleOutput`, ignores `--json`/`--quiet`.                                                   | Wrap in `createCommandResult` + `handleOutput`.                                            |
| M4  | `cli/hooks.ts:95-105`                                                                                                                                 | `hooks reinstall` shells out via `execFileSync` instead of calling `regenerateConfigs(projectRoot)`.                                        | Replace with direct call.                                                                  |
| M5  | `cli/agent-hooks.ts:423-428,445`                                                                                                                      | `parseAgentFlag(process.argv)` re-parses `--agent` after commander populated `opts.agent`. Dead.                                            | Delete; use `opts.agent`.                                                                  |
| M6  | 11 `index.ts` files in `src/core/`                                                                                                                    | Barrel re-exports violate the project's own `codi-typescript` rule.                                                                         | Import directly; delete barrels.                                                           |
| M7  | `core/security/content-scanner.ts:scanDirectory` (133 lines), `core/docs/doc-sync.ts:ensureDocProjectDir` (120), `docs-generator.ts:fixDocSync` (100) | Functions far exceed 30-line `codi-code-style` limit.                                                                                       | Split into validate / collect / write helpers.                                             |
| M8  | `src/core/external-source/{connectors,installer}.ts`                                                                                                  | 19 `throw new Error` coexist with 116 `Result<T>` returns. `installer.ts:80,120` throws inside `Result`-returning context.                  | Convert to `Result` or document as programmer-error-only.                                  |
| M9  | `core/hooks/version-bump-template.ts:20`, `migration/v2-to-v3.ts:45-51`                                                                               | Hardcoded `.codi/`, `artifact-manifest.json` repeated in 6+ files.                                                                          | Add `CONFIG_DIRNAME`, `ARTIFACT_MANIFEST_FILENAME` to `#src/constants.js`.                 |
| M10 | `core/skill/{evals-manager,feedback-collector}.ts`                                                                                                    | `writeEvals`, `readRuleFeedback` only used by own unit test.                                                                                | Move to `_internal.ts` or wire into CLI.                                                   |
| M11 | `runtime/brain-ui/pages/sessions.ts:143-150`                                                                                                          | `loadMetrics` p50/p90 percentile sort mislabels — on 1-row session picks index 0 identically; on 2-row p90=p50.                             | Use SQL `ORDER BY char_count LIMIT 1 OFFSET (n * 0.5)`; document as approximate when n<10. |
| M12 | `runtime/brain-ui/sse.ts:24-60`                                                                                                                       | Long-poll `while (!aborted)` never enforces max client lifetime. Wedged client keeps prepared-statement state indefinitely.                 | Add 1h idle cap + cleanup on `stream.onAbort`.                                             |
| M13 | `runtime/brain-ui/server.ts:39`                                                                                                                       | `busy_timeout = 5000` set after `applyMigrations` runs. Migration may throw before timeout takes effect.                                    | Set `busy_timeout` immediately after `openBrain()` returns.                                |
| M14 | `runtime/brain-ui/lifecycle.ts:13`, `cli-server.ts:25,48`                                                                                             | `DEFAULT_BRAIN_UI_PORT = 4477` + bind host `127.0.0.1` hardcoded; only `CODI_BRAIN_UI_BIND` env override.                                   | Add `CODI_BRAIN_UI_PORT` env override.                                                     |
| M15 | `src/templates/workflows/team-consolidation.yaml`                                                                                                     | All 4 non-terminal phases declare `chains: []` (empty). Satisfies contribution-discipline rule #4 syntactically but provides zero guidance. | Populate each phase's `chains:` (likely `team-consolidation-workflow` skill + helpers).    |
| M16 | `src/templates/skills/dev-team-consolidation-workflow/`                                                                                               | User-invocable skill missing `evals/evals.json`. Violates contribution-discipline rule #2.                                                  | Add at least one eval case.                                                                |
| M17 | `tests/runtime/recommend-pattern-linter.test.ts:31`                                                                                                   | `it.skip` with comment "Update in Sprint 3" — no ticket ref.                                                                                | Convert to `it.todo` with issue link or delete.                                            |
| M18 | `tests/unit/cli/preset.test.ts:254`                                                                                                                   | `it.skip("requires git clone — not tested")` — placeholder.                                                                                 | Delete; document gap in coverage exclude list.                                             |
| M19 | `tests/e2e/precommit-multilanguage.test.ts:18`, `tests/agent-harness/{tdd,using-codi}.test.ts`                                                        | `describe.skipIf` gated on env without CI signal — silent skips in default runs.                                                            | `console.warn` when skipped; fail in CI when harness env missing.                          |
| M20 | `tests/runtime/cli-handlers.test.ts` (778), `tests/unit/adapters/{claude-code:714,copilot:702}.test.ts`                                               | Test files exceed 700 LOC.                                                                                                                  | Split by command/adapter sub-feature.                                                      |
| M21 | `tests/integration/{self-introspection,full-pipeline,skill-management}.test.ts` + `unit/cli/init.test.ts`                                             | Same `vi.mock("#src/core/version/template-hash-registry.js")` copy-pasted in 4 files.                                                       | Extract `tests/fixtures/template-hash-registry-mock.ts`.                                   |
| M22 | `tests/unit/adapters/__snapshots__/output-snapshots.test.ts.snap` (291 lines)                                                                         | Largest snapshot — risk of blind updates.                                                                                                   | Replace with targeted assertions.                                                          |

## LOW / INFO (selected, 25 total)

| #   | Item                                                                                          | Note                                                                                                                                                                                                                                                                                                                   |
| --- | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- |
| L1  | `scripts/backfill-tokens.ts:4` + `migrate-v5.ts:4`                                            | Hardcoded absolute path `/Users/laht/projects/codi/.codi/state/brain.db`. **Fix: use `process.cwd()`.**                                                                                                                                                                                                                |
| L2  | Orphan scripts: `backfill-tokens.ts`, `migrate-v5.ts`, `test-gates.ts`, `test-iron-law-7.ts`  | Not referenced in `package.json`/CI. One-shot migrations belong in git history. **Fix: delete or move to `scripts/dev/`.**                                                                                                                                                                                             |
| L3  | `package.json:111` `"codi-cli": "^0.9.2"` in `devDependencies`                                | Self-dependency — shadows dev work with stale 0.9.2. **Fix: remove.**                                                                                                                                                                                                                                                  |
| L4  | `tsconfig.json:25-31` excludes `tests`                                                        | Tests not type-checked. **Fix: add `tsconfig.test.json` extending base.**                                                                                                                                                                                                                                              |
| L5  | `eslint.config.js:20-23` ignores `src/runtime/**` + `tests/runtime/**` + `scripts/runtime/**` | Sprint-2 lint debt — large surface bypassing `no-explicit-any`. **Fix: open tracking issue, remove incrementally.**                                                                                                                                                                                                    |
| L6  | `.codi_output/` (196K) + `.codi_output_BKP/` (456K) on disk                                   | Untracked but stale (BKP from Apr 16). **Fix: `rm -rf`.**                                                                                                                                                                                                                                                              |
| L7  | `.DS_Store` in 10 directories                                                                 | Leaks to subfolders. **Fix: add `.DS_Store` to root `.gitignore` + `git rm --cached **/.DS_Store`.\*\*                                                                                                                                                                                                                 |
| L8  | `audit-0[1-5]-*.png` (5 PNGs, ~180KB) in repo root                                            | Stale artifacts. **Fix: move to `docs/audit-screens/` or delete.**                                                                                                                                                                                                                                                     |
| L9  | `PENDING.md` (56KB, last touched Apr 18)                                                      | Stale tracking doc. **Fix: archive or delete.**                                                                                                                                                                                                                                                                        |
| L10 | Files >700 LOC (10 total)                                                                     | `cli/workflow.ts` 799, `cli/init.ts` 799, `cli/update.ts` 798, `codi-brand/site-app.js` 796, `brain-ui/pages/sessions.ts` 780 (NEW), `skill-creator/template.ts` 775, `init-wizard-paths.ts` 755, `content-factory/template.ts` 734, `hook-templates.ts` 716, `contribute.ts` 712. **Fix: split by subcommand/phase.** |
| L11 | `cli/wizard-legend.ts`                                                                        | Only used by its own test. **Fix: delete or wire into wizard.**                                                                                                                                                                                                                                                        |
| L12 | `cli/agent-hooks.ts:452` `void hook;` no-op                                                   | **Fix: remove.**                                                                                                                                                                                                                                                                                                       |
| L13 | `cli/prefs.ts:260` `void runPrefsInit;`                                                       | Defensive tree-shake stub. **Fix: remove (already used at line 170).**                                                                                                                                                                                                                                                 |
| L14 | `cli/migrate.ts:94-101` `[direction]` positional with only valid value `"v2-to-v3"`           | Effectively unused. **Fix: drop argument.**                                                                                                                                                                                                                                                                            |
| L15 | `runtime/brain-ui/routes-api.ts:187-217` PATCH `/captures/:id`                                | Accepts arbitrary `type` strings — not validated against `CAPTURE_TYPES`. **Fix: reject non-canonical with 400.**                                                                                                                                                                                                      |
| L16 | `runtime/capture/session.ts:38-52` `execGit` × 3 per `ensureProject`                          | First-time session start stalls up to 3s. \*\*Fix: single `git config --get-regexp '^(user                                                                                                                                                                                                                             | remote\.origin\.url)'`.\*\* |
| L17 | `runtime/workflows/registry.ts:20-26`                                                         | `quick` + `team-consolidation` types only in comment as exclusions. **Fix: `QUICK_MODE_TYPES` set; typed `{ kind: "quick" }` return.**                                                                                                                                                                                 |
| L18 | `tests/unit/utils/paths.test.ts:21` reads real `os.homedir()`                                 | **Fix: inject `HOME` via fixture.**                                                                                                                                                                                                                                                                                    |
| L19 | `vitest.config.ts:81-117`                                                                     | 30+ files in coverage `exclude` tagged "test-debt" without ticket refs. **Fix: add issue numbers per entry.**                                                                                                                                                                                                          |
| L20 | `vitest.config.ts:120-153` thresholds 75/66/79/76                                             | Below project rule of 80% min for business logic. **Fix: raise or document exception.**                                                                                                                                                                                                                                |
| L21 | `pyproject.toml:4` `requires-python = ">=3.14"`                                               | Bleeding edge (Oct 2025). **Fix: document in CONTRIBUTING or relax to `>=3.12`.**                                                                                                                                                                                                                                      |
| L22 | `package.json:45` `prepublishOnly` with `[ "$CI" = "true" ]` short-circuit                    | Local main pushes skip guard if `CI=true` set. **Fix: audit bypass — `CI=true` should not skip branch check.**                                                                                                                                                                                                         |
| L23 | `.husky/pre-commit:22` `npx eslint --fix \| xargs`                                            | Pathological staged-file lists. **Fix: `xargs -L 50` or `lint-staged`.**                                                                                                                                                                                                                                               |
| L24 | `src/templates/skills/dev-agent-creator/references/agent-authoring.md`                        | Reference file never mentioned in `template.ts`. **Fix: link from template or delete.**                                                                                                                                                                                                                                |
| L25 | `src/core/version/template-hash-registry.ts:127` `_resetRegistryCache`                        | Test-only helper. **Fix: do not expose from any barrel; acceptable.**                                                                                                                                                                                                                                                  |

## False positives clarified

| Initial flag                                                           | Verdict                                                                                                           |
| ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `node_modules/` inside `src/templates/skills/content-factory/scripts/` | NOT tracked — `.gitignore` covers; 3125 files / 29 MB local-only. No git action needed.                           |
| `src/templates/skills/dev-using-codi/` missing `evals/evals.json`      | Skill is `internal: true` + `disable-model-invocation: true` — never triggers via classifier. Exempt.             |
| `src/templates/workflows/quick.yaml` missing `chains:`                 | Workflow declares `flags.no_phase_chain: true` + `auto_generate_phase_refs: false`. Intentional opt-out.          |
| `process.exit()` count 206                                             | Concentrated in CLI entry points (correct callsite); only `hook-templates.ts` non-CLI use warrants consolidation. |
| `any`/`as any` usage                                                   | 16 explicit (6 with `eslint-disable` + rationale in `schema-renderers.ts`). Compliant.                            |
| Spanish in identifiers                                                 | 0 — only `templates/rules/spanish-orthography.ts` (correct: rule definition).                                     |
| Real `FIXME`/`HACK`/`XXX` in production                                | 0 — found tokens are skill prompt text or regex pattern strings.                                                  |

## Recommended remediation order

1. **C1** (path-traversal in brain-ui) — security blocker, ~1h
2. **H3, H4** (exit code propagation in `brain.ts`, `migrate.ts`) — silent CI failures, ~30min
3. **H1, H2** (exec timeouts) — hang prevention, ~2h shared wrapper
4. **H12, H14** (Zod validation + transactional Stop hook) — data integrity, ~3h
5. **H6, H7, H8, H15** (4 duplication clusters) — net code reduction, ~6h
6. **H9** (frontmatter parser unification) — ~2h, blocks future refactors
7. **H16, H17** (rewrite internal-mock tests) — ~4h, raises real coverage
8. **L1, L2, L6, L7, L8, L9** (repo hygiene) — ~30min batch cleanup
9. Remaining MED/LOW items — incremental, can ship across sprints

## Notes for future audits

- `src/runtime/brain-ui/pages/sessions.ts` (780 LOC) is **new** since the last >700 list — CLAUDE.md soft limit was crossed without trigger
- 6 files cluster around the 700-LOC boundary; consider a CI check that fails on any new file >700 LOC
- `vitest.config.ts` coverage exclude list grows unchecked — add per-entry issue link requirement to the contribution-discipline lint
