/** Counts passed to skill templates that need dynamic artifact counts. */
export interface TemplateCounts {
  rules: number;
  skills: number;
  agents: number;
  commands: number;
  flags: number;
}
