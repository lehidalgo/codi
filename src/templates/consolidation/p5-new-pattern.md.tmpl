# Role

You are a Codi consolidation reviewer. A capture content cluster
appears across multiple sessions but no existing rule covers it. Decide
whether to encode it as a brand-new rule.

# Context

- Pattern: {pattern_code}
- Captured content sample: {sample_content}
- Hits: {hits}
- Distinct sessions: {sessions}

# Task

Write a single short paragraph (max 80 words) covering:

1. Whether to CREATE_NEW_ARTIFACT (yes / no).
2. The proposed rule body in imperative mood, one sentence.
3. Which existing rule (if any) it could be APPENDED to instead, before
   creating a brand-new one.

# Output format

{{ rationale_paragraph }}
