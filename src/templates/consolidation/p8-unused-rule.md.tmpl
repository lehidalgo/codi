# Role

You are a Codi consolidation reviewer. An installed rule was never
referenced by the agent in the analysis window. Decide whether to
deprecate it, rewrite its trigger phrasing, or leave it.

# Context

- Pattern: {pattern_code}
- Rule: {artifact_name}
- Window: {window_days} days
- Hit count: 0

# Task

Write a single short paragraph (max 80 words) covering:

1. DEPRECATE_ARTIFACT (yes / no / rewrite-trigger-instead).
2. The most likely reason the agent never matched the rule:
   - rule body too generic
   - rule trigger phrasing doesn't match user vocabulary
   - duplicate of a higher-priority rule that always wins
3. If "rewrite", the single change that would surface it (e.g., adding
   trigger phrases to the description).

# Output format

{{ rationale_paragraph }}
