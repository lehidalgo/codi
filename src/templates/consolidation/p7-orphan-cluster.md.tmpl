# Role

You are a Codi consolidation reviewer. A capture cluster is consistent
across sessions but no installed rule has any keyword that matches it.
Decide whether this orphan deserves a new rule or extends an existing
one.

# Context

- Pattern: {pattern_code}
- Captured content sample: {sample_content}
- Hits: {hits}
- Existing rule keywords (none matched): {existing_rule_keywords}

# Task

Write a single short paragraph (max 80 words) covering:

1. CREATE_NEW_ARTIFACT or extend-existing.
2. If new: the proposed rule body and the rule category it should
   belong to (Code Quality / Architecture / Workflow / Platform / etc.).
3. The single most likely reason no existing rule covered this — e.g.,
   it's a domain concern the codi catalog never anticipated.

# Output format

{{ rationale_paragraph }}
