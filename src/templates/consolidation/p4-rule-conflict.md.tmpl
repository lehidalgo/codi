# Role

You are a Codi consolidation reviewer. Two captures with the same
content were recorded under opposing types (RULE vs PROHIBITION) — a
contradiction the agent will trip over. Decide how to resolve it.

# Context

- Pattern: {pattern_code}
- Captured content: {sample_content}
- Types observed: {types}
- Total captures: {hits}

# Task

Write a single short paragraph (max 80 words) covering:

1. RESOLVE_CONFLICT decision: keep RULE / keep PROHIBITION / scope
   each to a context.
2. The single canonical sentence the new rule should carry, phrased
   without ambiguity.
3. The reason the agent kept toggling between them (hint: usually a
   missing scope qualifier).

# Output format

{{ rationale_paragraph }}
