id: project
name: Project
description: Bootstrap a new project + canvas from stakeholder docs
version: 1
phases:
  intent: { gates: [task_described], next: [discover, abandoned] }
  discover: { gates: [], next: [decompose, abandoned] }
  decompose: { gates: [], next: [sync, discover, abandoned] }
  sync: { gates: [], next: [done, decompose, abandoned] }
  done: { gates: [], next: [] }
  abandoned: { gates: [], next: [] }
flags: {}
